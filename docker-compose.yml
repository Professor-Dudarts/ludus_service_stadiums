version: '3'
services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 80:80
    depends_on:
      - db
    environment:
      - ConnectionStrings__DefaultConnection=Server=db;Database=LudusStadium;User=sa;Password=admin
  db:
    image: mcr.microsoft.com/mssql/server
    environment:
      - SA_PASSWORD=admin
      - ACCEPT_EULA=Y
